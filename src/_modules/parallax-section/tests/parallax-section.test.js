'use strict';

var ParallaxSection = require('../parallax-section');

describe('ParallaxSection View', function() {

  beforeEach(function() {
    this.parallaxSection = new ParallaxSection();
  });

  it('Should run a few assertions', function() {
    expect(this.parallaxSection);
  });

});
