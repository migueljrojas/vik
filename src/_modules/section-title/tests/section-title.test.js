'use strict';

var SectionTitle = require('../section-title');

describe('SectionTitle View', function() {

  beforeEach(function() {
    this.sectionTitle = new SectionTitle();
  });

  it('Should run a few assertions', function() {
    expect(this.sectionTitle);
  });

});
