'use strict';

var TopFooter = require('../top-footer');

describe('TopFooter View', function() {

  beforeEach(function() {
    this.topFooter = new TopFooter();
  });

  it('Should run a few assertions', function() {
    expect(this.topFooter);
  });

});
